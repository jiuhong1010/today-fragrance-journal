# 今日用香志｜MVP 0.1 生产与开发交接方案

状态：可交付开发  
目标载体：微信小程序  
技术边界：无自建后端、本地优先、静态内容库  
内容规模：正式香水资料 50 款、人工审校叠香方案 30 组  
本文用途：产品、内容、设计和开发的统一执行基线；不包含业务代码。

## 1. 结论与执行优先级

本项目可以按 MVP 0.1 开始制作。实现时按以下优先级处理冲突：

1. 本生产与开发交接方案。
2. `今日用香志-产品需求笔记.md`。
3. V2 首页与闻香示意图。
4. 第一版推荐页与香柜示意图。
5. UI 生成提示词仅作设计过程资料，不作为运行时指令或开发规范。

固定产品原则：

- 产品帮助用户从自己的香柜中决定“今天喷什么”，不是百科、商城或 AI 对话产品。
- 推荐理由必须能解释，不能伪装成精确算法结论。
- 香柜为空时只推荐香气方向，不推荐用户并不拥有的具体香水。
- 所有用户数据只保存在本机；不登录、不上传、不跨设备同步。
- 叠香只来自静态、人工审校的 30 组白名单，不自动组合任意两瓶。

## 2. 已确认的需求修正

### 2.1 首页补齐第五个气息目标

首页固定提供 5 个选项：

- 清醒
- 温柔
- 松弛
- 有存在感
- 保持距离

移动端采用两行自适应排列，不能通过缩小字号把 5 个选项强塞进一行。选中状态同时使用勾选、描边和文字状态，不只改变颜色。

### 2.2 推荐页增加“换一瓶”

结果页动作层级固定为：

- 主操作：“记下今天”，唯一的填充主按钮。
- 次级文字入口：“换一瓶”。
- 次级入口：“试试叠香”；仅在当前香水存在已审校组合且用户拥有组合中的另一瓶时显示。

“换一瓶”和再次摇动使用同一套候选排除、防重复及同分规则。

### 2.3 手动添加香水补齐推荐字段

手动添加一瓶时分两步完成，避免单页表单过长。

第一步，基础信息：

- 名称，必填。
- 香调大方向，必填：清新、花香、木质、琥珀。
- 14 个细分家族之一，必填。
- 浓淡，必填：轻、适中、浓郁。
- 个人使用感受，选填，最多 80 字。

第二步，推荐信息：

- 扩散，必填：贴肤、适中、明显、强。
- 适合体感温度，至少选择 1 项：闷热、温暖、微凉、寒冷。
- 适合场合，至少选择 1 项：通勤、约会、聚会、独处、正式场合。
- 气质标签，至少选择 1 项：清醒、温柔、松弛、有存在感、保持距离。
- 强势特征，选填多选：高甜、浓白花、烟熏、皮革、辛辣、粉感、动物感、无。
- 三个关键香材，选填；用户不确定时允许为空，不据此生成事实性描述。

系统可根据细分家族给出默认建议，但必须让用户确认。字段未完成的自定义香水可以保存在香柜中，但状态标记为“待补充”，不进入推荐和叠香候选。

### 2.4 本地数据提示文案

首次记录香柜时展示一次轻提示，设置页长期展示：

> 你的香柜、记录和偏好只保存在当前设备。更换设备、删除小程序数据或清理缓存后无法恢复。

“清除我的数据”需要二次确认：

> 将删除香柜、用香记录、手动添加的香水和本地偏好。内置香水资料不会删除，操作无法撤销。

## 3. 页面与状态清单

### 3.1 底部导航

固定为三个入口：

1. 今日
2. 香柜
3. 闻香

结果页、添加页、历史页、详情页和设置页不占用底部导航。

### 3.2 今日

首页组件顺序固定为：

1. “先选想留下的气息”，5 个气息目标。
2. “再告诉我今天的环境”。
3. 场合选择。
4. 体感温度选择。
5. 提示“气息决定方向，环境帮你校正。”
6. 主按钮“摇一摇，选出今天的味道”。

必须支持点击按钮完成全流程；摇一摇只是等价的仪式性交互。传感器失败、系统不支持、用户未摇动或开启减少动态效果时，不影响按钮入口。

### 3.3 今日推荐

固定展示：

- 今日主题。
- 香水名称；内置香水显示事实名称，自定义香水显示用户填写名称。
- 一个香调家族和最多三个关键香材，两类信息使用不同标签。
- 两句推荐理由：第一句解释目标，第二句解释场合或温度约束。
- 具体喷法：位置、喷数、是否建议补喷。
- 四维感受：清冽/柔和、贴肤/扩散、干燥/甜润、日常/戏剧。
- “换一瓶”“记下今天”“试试叠香”。

自定义香水没有可靠香材数据时，不显示香材事实，不根据香调家族臆造具体香材。

### 3.4 香柜

包含：

- 全部、清新、花香、木质、琥珀筛选。
- 列表优先展示距上次使用天数。
- 内置香水添加入口。
- 手动添加入口。
- 待补充记录入口。
- 最近 30 次用香记录。

空状态提供：

- “我有香水，开始添加”
- “我还不懂，先认识香调”

### 3.5 闻香

保持 V2 示意图结构：四个大方向、14 个细分家族、搜索、家族展开和一分钟科普。搜索只查本地静态内容，不请求网络。

### 3.6 设置与隐私

包含：

- 本地数据说明。
- 当前数据版本和内容库版本。
- 清除我的数据。
- 减少动态效果开关；默认跟随系统，可由用户覆盖。
- 内容来源与免责声明。

## 4. 数据模型

### 4.1 内置香水记录

正式内容库恰好包含 50 条记录。每条包含：

| 字段 | 要求 |
|---|---|
| `id` | 稳定且不可复用，建议 `brand-product-concentration` 形式 |
| 品牌与产品名 | 中英文分开存储；事实名称，不改写 |
| 浓度版本 | EDT、EDP、Parfum 等；不同版本不得混为一条 |
| 4 大方向 | 单选 |
| 14 个细分家族 | 单选 |
| 关键香材 | 1～3 个，每个带小白解释和来源状态 |
| 浓淡 | 1～3 级 |
| 扩散 | 1～4 级 |
| 留香提示 | 短、适中、较长；不承诺具体小时数 |
| 适温 | 闷热、温暖、微凉、寒冷，多选 |
| 场合 | 通勤、约会、聚会、独处、正式场合，多选 |
| 气质标签 | 5 个首页目标，多选，并区分主要/次要 |
| 强势特征 | 高甜、浓白花、烟熏、皮革、辛辣、粉感、动物感等 |
| 四维感受 | 4 个 0～100 的整数值 |
| 默认喷法 | 按场合给出位置、喷数和补喷提示 |
| 内容文案 | 推荐理由素材，不复制品牌宣传文案 |
| 事实来源 | 品牌官方产品页优先；URL、查询日期、审校人 |
| 内容状态 | 草稿、已核对、已审校、可发布 |

只有“可发布”记录进入正式包。品牌页面未明确的信息必须标记为编辑判断，不能包装成品牌事实。

### 4.2 用户香柜记录

用户香柜不复制整条内置资料，只保存：

- 香水 `id` 或自定义香水 `customId`。
- 添加时间。
- 上次使用时间。
- 使用次数。
- 是否归档。
- 用户个人备注。

### 4.3 自定义香水记录

保存第 2.3 节的全部字段，并额外保存：

- `customId`。
- `createdAt`、`updatedAt`。
- `profileComplete`。
- 字段来源统一标记为“用户填写”。

### 4.4 用香记录

每条包含：

- 记录 ID 和日期时间。
- 主香 ID。
- 可选辅香 ID。
- 当日气息目标、场合、体感温度。
- 喷法快照。
- 评价：很适合、一般、翻车。
- 80 字以内短记。
- 当时使用的推荐规则版本。

保存喷法和规则快照，避免内容库升级后历史记录含义改变。

### 4.5 叠香白名单记录

正式内容库恰好包含 30 条组合。每条包含：

- 稳定组合 ID。
- 主香 ID、辅香 ID；两者必须都来自 50 款内置资料。
- 推荐顺序。
- 默认各自喷数。
- 分区位置。
- 适温、适合场合、适合气息目标。
- 预期变化。
- 风险提示。
- 禁用或慎用条件。
- 试喷记录和审校状态。

主辅顺序不同但效果相同的组合只算 1 组。只有“可发布”组合能在产品中出现。

## 5. 推荐规则 V1

### 5.1 基本约定

- 推荐规则版本固定记录为 `recommendation-v1`。
- 只对用户香柜中的完整记录计算。
- 首页目标权重最高；场合和体感是环境约束。
- 结果来自规则和有限随机，不使用网络模型或生成式 AI。

### 5.2 场合对扩散上限

扩散分为 1 贴肤、2 适中、3 明显、4 强。

| 场合 | 默认扩散上限 | 说明 |
|---|---:|---|
| 通勤 | 2 | 避免先于使用者进入共享空间 |
| 正式场合 | 2 | 保持克制和可控距离 |
| 约会 | 3 | 允许近距离存在感 |
| 聚会 | 4 | 可以更鲜明 |
| 独处 | 4 | 由个人偏好决定 |

强度超出上限 1 级时不直接淘汰，但施加高惩罚并把喷法限制为 1 喷；超出 2 级时视为明显不合适，进入环境过滤。

### 5.3 候选与过滤

依次执行：

1. 只取香柜中未归档且字段完整的香水。
2. 排除明显违反场合扩散上限的候选。
3. 排除与所选体感温度完全不相容的候选。
4. 若没有候选，不擅自改变气息目标，进入“无候选处理”。

体感温度按相邻关系处理：闷热—温暖—微凉—寒冷。相邻一档可以保留并扣分，跨两档视为明显不相容。

### 5.4 排序分数

通过过滤的候选按 100 分基础规则排序：

| 项目 | 分值 |
|---|---:|
| 气息目标为主要标签 | +40 |
| 气息目标为次要标签 | +24 |
| 场合明确适配 | +20 |
| 体感温度明确适配 | +15 |
| 体感温度为相邻档 | +7 |
| 最近同类记录为“很适合” | +12 |
| 最近同类记录为“翻车” | -25 |
| 7～29 天未使用 | +4 |
| 30 天以上未使用 | +8 |
| 今天已使用 | -35 |
| 3 天内已使用 | -16 |
| 7 天内已使用 | -7 |
| 扩散超出场合上限 1 级 | -18 |

“一般”不加减分。用户反馈只作用于同一香水在相同气息目标或相同场合下的记录，避免一次翻车永久压低所有场景。

### 5.5 同分规则

分差不超过 2 分视为同分，按以下顺序决定：

1. 更久未使用者优先。
2. 历史“很适合”比例更高者优先；少于 3 条反馈时不比较比例。
3. 仍同分时，用“当天日期 + 香水 ID”生成稳定顺序；同一天重复打开不会无故跳动。

### 5.6 有限随机

第一次结果不固定选择最高分，而是在最高分 8 分以内的候选池中按分数加权抽取。这样保留“选出今天味道”的仪式感，同时避免低匹配候选意外胜出。

候选池少于 3 瓶时不额外扩池。

### 5.7 连续摇动与防重复

- 单次摇动手势只触发一次，触发后冷却 1.2 秒。
- 10 分钟内视为同一轮选择。
- 香柜有 3 瓶及以上时，排除本轮最近出现的 2 瓶。
- 香柜只有 2 瓶时，只排除当前展示的 1 瓶。
- 香柜只有 1 瓶时，不伪装成随机；直接给出该瓶是否适配及调整喷法。
- 候选全部展示过后重置本轮池，但仍不连续重复当前这瓶。
- 点击“换一瓶”与摇一摇共享上述状态。
- 页面离开超过 10 分钟或跨自然日后开始新一轮。

### 5.8 无候选处理

分四种状态：

1. **香柜为空：**推荐一个香气方向，提供“添加我的香水”和“先认识香调”。
2. **只有待补充记录：**提示完成扩散、适温、场合和气质字段，不生成具体推荐。
3. **环境过滤后为空：**保持气息目标不变，建议用户只放宽一项环境条件；优先放宽体感的相邻一档，再考虑场合扩散限制。
4. **只有一瓶且不完全合适：**明确说明限制，给出保守喷法；如果超出扩散上限 2 级或温度跨 2 档，则不声称“适合”。

固定文案框架：

> 你的香柜里暂时没有同时符合“{目标}”和“{环境}”的香水。可以放宽“{建议条件}”，或今天先少量使用更接近的一瓶。

### 5.9 推荐理由生成

使用审校过的短句模板，不自由生成事实。每次固定两句：

1. `{关键香材或感受特征}符合你今天想要的“{气息目标}”。`
2. `{扩散/浓淡/温度说明}，适合{体感温度}的{场合}。`

自定义香水无可靠关键香材时，第一句只使用用户确认的家族、气质和浓淡字段。

## 6. 叠香规则 V1

叠香入口同时满足以下条件才出现：

1. 用户香柜至少有 2 瓶。
2. 当前主香与香柜中的另一瓶存在于 30 组白名单。
3. 该组合适配当前场合和体感温度。
4. 两瓶最近没有在相同场景被用户记录为“翻车”；若有，入口仍可在详情查看，但不主动推荐。

结果必须展示：主香、辅香、顺序、各自喷数、分区位置、预期变化和风险。默认总量为 2 喷；两瓶扩散都达到 3 级时，室内场合默认总量降为 1～2 喷并强提示分区试喷。

固定免责声明：

> 香气表现会受皮肤、温度、距离和个人感知影响。第一次组合请低剂量、分区试喷。

## 7. 本地存储、版本与迁移

### 7.1 数据分层

- 50 款香水与 30 组叠香随小程序静态发布，只读，使用独立 `contentVersion`。
- 用户香柜、自定义香水、用香记录、偏好和选择轮次保存在本地，使用 `schemaVersion`。
- 静态内容升级不得覆盖用户数据。

### 7.2 建议的本地键空间

所有键统一使用产品前缀，禁止使用无命名空间的通用键：

- 元信息
- 用户香柜
- 自定义香水
- 用香记录
- 偏好设置
- 推荐轮次状态
- 迁移临时备份

开发实现时应逐项删除本产品键，不直接调用全量清空作为常规“清除我的数据”实现。

### 7.3 初始版本

- `schemaVersion = 1`
- `contentVersion = 1`
- `recommendationRuleVersion = recommendation-v1`

### 7.4 迁移流程

每次启动依次执行：

1. 读取并校验元信息。
2. 如果数据版本落后，先复制用户数据到迁移临时备份。
3. 在副本上按版本逐级迁移，不能跨版本跳跃。
4. 校验 ID、必填字段、记录数量和时间格式。
5. 校验通过后一次性切换到新数据并更新版本号。
6. 切换成功后删除临时备份。
7. 任一步失败则保留旧数据，回滚并提示用户重试；不能用空数据覆盖。

每个版本必须提供迁移测试样本，包括：空数据、正常数据、缺字段数据、重复 ID、损坏记录和接近容量上限的记录。

### 7.5 清除机制

清除用户数据时删除：香柜、自定义香水、用香记录、偏好、轮次状态和临时备份；保留随应用发布的 50 款资料与 30 组叠香。清除成功后回到香柜空状态，并再次显示本地数据说明。

## 8. 50 款香水内容生产方案

### 8.1 家族配额

内容库最终恰好 50 款，按主家族分配：

| 大方向 | 细分家族 | 数量 |
|---|---|---:|
| 清新 | 柑橘 | 4 |
| 清新 | 水生 | 3 |
| 清新 | 绿意 | 4 |
| 清新 | 果香 | 4 |
| 花香 | 柔和花香 | 4 |
| 花香 | 经典花香 | 5 |
| 花香 | 花香琥珀 | 4 |
| 琥珀 | 柔和琥珀 | 3 |
| 琥珀 | 经典琥珀 | 3 |
| 琥珀 | 木质琥珀 | 4 |
| 木质 | 纯木质 | 4 |
| 木质 | 苔藓木质 | 3 |
| 木质 | 干燥木质 | 3 |
| 木质 | 芳香草本 | 2 |
| **合计** |  | **50** |

### 8.2 扩散配额

- 贴肤：6 款。
- 适中：14 款。
- 明显：18 款。
- 强：12 款，其中真正高风险强扩散不超过 4 款。

同一款可适配多个温度和场合，但最终库必须保证：

- 每种体感温度至少有 15 个适配候选。
- 每种场合至少有 18 个适配候选。
- 每个气息目标至少有 16 个主要或次要候选。
- 通勤和正式场合合计至少有 16 款扩散不高于 2 级的候选。

### 8.3 选品原则

- 优先选择用户较容易拥有或认识、且官方资料仍可核对的作品。
- 同一品牌原则上不超过 3 款，避免内容库变成品牌陈列。
- 不使用“男香/女香”决定分类。
- 不把不同浓度版本合并。
- 不因热度牺牲家族、温度、扩散和场合覆盖。
- 商品名可以用于事实识别，视觉只使用原创抽象瓶形，不使用品牌瓶身图和商标。

### 8.4 内容生产流程

每款依次通过：

1. 候选登记和配额检查。
2. 品牌官方页面核对名称、浓度和公开香材。
3. 必要时使用第二权威来源交叉确认。
4. 编辑成小白能理解的原创短句。
5. 产品人员填写场合、温度、扩散和气质判断，并明确这是编辑判断。
6. 第一位审校人核事实，第二位审校人核文案和推荐一致性。
7. 进入真机内容检查。
8. 状态改为“可发布”。

50 条全部可发布之前，不能用未审校占位内容凑数上线。

## 9. 30 组叠香内容生产方案

### 9.1 组合配额

建议分布：

| 组合方向 | 数量 |
|---|---:|
| 清新 + 木质 | 8 |
| 柑橘/绿意 + 花香 | 6 |
| 花香 + 木质 | 5 |
| 花香 + 琥珀 | 4 |
| 果香 + 干燥木质/芳香草本 | 4 |
| 水生 + 芳香草本/木质 | 3 |
| **合计** | **30** |

覆盖约束：

- 30 组必须全部来自上述 50 款香水。
- 至少覆盖 36 款不同香水。
- 同一款最多出现在 3 组中。
- 至少 18 组适合室内低剂量使用。
- 至少 8 组适合闷热或温暖体感。
- 至少 8 组适合微凉或寒冷体感。
- 高甜 + 高扩散、高扩散 + 高扩散不作为默认推荐组合。

### 9.2 审校与试喷

每组至少完成：

1. 纸上结构审查。
2. 低剂量分区试喷。
3. 至少 2 个温度环境或室内/室外条件记录。
4. 至少 3 位体验者反馈；30 组整体测试人群不少于 5～10 人。
5. 明确写出失败风险，不只写正向结果。
6. 两位审校人确认主辅顺序、总喷数和场合边界。

内容无法形成保守、可解释结论时，从白名单移除并补做另一组，不发布模糊建议。

## 10. 视觉与交互实现约束

- 以 V2 首页和闻香图为主，第一版图只补充推荐页和香柜结构。
- 背景、文字和强调色沿用需求笔记中的 OKLCH 令牌；开发时同时提供不支持 OKLCH 环境的等效色值。
- 主按钮单屏最多一个。
- 正文、按钮和导航使用系统无衬线；宋体只用于每屏一个短标题。
- 不依赖外置大体积中文字体包。
- 普通容器 12～16px 圆角，不做卡片套卡片。
- 低对比度次级文字也必须在真机日间环境下可读。
- 动效 150～250ms；摇一摇揭晓只做一次遮罩聚合和线条展开。
- 开启减少动态效果后改为直接交叉淡入。
- 原创瓶形建议使用一组轻量抽象素材并做有限变体，控制主包体积。

## 11. 建议技术实现边界

默认采用原生微信小程序 + TypeScript；如果后续已有明确工程框架，则在不改变本文产品规则的前提下适配。

模块边界建议拆为：

1. 静态内容读取与版本。
2. 本地数据仓库与迁移。
3. 推荐规则引擎。
4. 叠香白名单匹配。
5. 摇一摇手势识别与按钮等价入口。
6. 页面和组件。
7. 内容校验工具与测试夹具。

推荐逻辑应为无界面依赖的纯规则模块，便于用固定输入验证结果。文案模板、内容数据和评分规则不要散落在页面代码中。

## 12. 开发阶段与交付物

### 阶段 A：工程骨架和数据契约

交付：页面路由、设计令牌、字段模型、静态内容校验、空的 50/30 内容槽位、版本元信息。

退出条件：数据校验能阻止数量错误、重复 ID、缺必填字段和未审校内容进入发布包。

### 阶段 B：本地数据与推荐引擎

交付：香柜、自定义香水、记录、评分、无候选、同分、连续摇动和防重复规则，以及版本迁移。

退出条件：规则测试覆盖 0、1、2、3、10 瓶香柜和全部边界状态。

### 阶段 C：核心页面

交付：今日、推荐、香柜、闻香、添加、历史、设置与清除数据流程。

退出条件：无需传感器，用户也能在 60 秒内完成首次推荐。

### 阶段 D：摇一摇、动效和分享卡

交付：传感器启停、手势去抖、减少动态效果、失败回退和本地分享卡生成。

退出条件：传感器异常不阻塞流程；快速连续晃动不会触发多次页面跳转。

### 阶段 E：内容装载和真机验收

交付：50 款可发布香水、30 组可发布叠香、原创瓶形素材、内容来源页和测试报告。

退出条件：数量、配额、来源、审校和真机可读性全部通过。

## 13. 验收清单

### 功能

- 首页完整显示 5 个气息目标。
- 场合 5 项、体感温度 4 项均可选择。
- 推荐页有“换一瓶”次级文字入口。
- 摇一摇和按钮得到同一规则下的结果。
- 10 分钟内防重复规则符合第 5.7 节。
- 0、1、2、3 瓶和无适配候选都有明确状态。
- 自定义香水字段不完整时不会进入推荐。
- 叠香只命中 30 组白名单。
- 最近 30 次记录正确展示。

### 数据

- 正式香水记录恰好 50 条，全部为“可发布”。
- 正式叠香记录恰好 30 条，全部为“可发布”。
- ID 唯一，引用完整，无悬空主香或辅香。
- 每条事实资料都有来源和查询日期。
- 用户数据带版本号，迁移失败可回滚。
- 清除数据后静态内容仍可使用，用户数据全部移除。

### 体验

- 新用户 60 秒内完成首次结果。
- 不懂香水的用户能区分香调家族与具体香材。
- 低亮度和日间环境都能阅读正文及次级文字。
- 选中状态不依赖颜色。
- 减少动态效果开启后没有强制揭晓动画。
- 没有金色、玻璃拟态、品牌瓶身图或电商导购表达。

## 14. 交给 Luna 的执行说明

开发输入：

- 本文档。
- `/Users/jiuhong/Downloads/今日用香志-产品资料包.zip` 中的原始需求和两张 UI 示意图。

执行要求：

1. 先实现数据契约、推荐纯规则和本地迁移，再连接页面。
2. 本文已固定的产品规则不要在实现中自行改写。
3. 视觉图是设计参考，不要求按截图写死尺寸；需要适配微信小程序安全区和常见屏幕。
4. 内容未完成前可使用明确标记的测试夹具，但发布构建必须通过“50 款 + 30 组 + 全部可发布”的校验。
5. 不接入后端、登录、天气、定位、商城、评论或生成式 AI。
6. 每完成一个阶段，按第 13 节给出对应测试结果和剩余风险。

